<div class="breadcrumbs">
  <% locals?.breadcrumbs?.forEach((item, index) =>{ %>
  <a href="<%=item.base%>/<%=item.id%>" class="crumbs <%= locals.currentPage.base === item.base &&  currentPage.path  === item.id ?'crumbs--active': ''%>">
    <%=item.label%>
  </a>
  <% if (index < breadcrumbs.length - 1){ %>
  <span>=></span>
  <% }  %>
  <%})%>
</div>